# 검증 기록

검증일: 2026-09-09. 로컬 환경: Windows, Python 3.12.14.

## 자동 검증

| 항목 | 결과 |
| --- | --- |
| pytest | **58개 통과** |
| Ruff 정적 검사 | 통과 |
| Ruff Python 포맷 검사 | 통과 |
| JavaScript 구문 검사 | 통과 |
| Python 패키지 의존성 검사 | 충돌 없음 |
| 배포 wheel 생성 | 성공, HTML/CSS/JS/아이콘 포함 확인 |
| 비밀·로컬 DB 제외 | `.env`, `data/`, `.venv/`가 Git ignore 대상임을 확인 |

주요 시나리오:

- 요청한 **21개 목적지 필드의 값 전체** 비교, 원본 Part ID 보존과 앞 15자 추출.
- 월말·윤년·연도 경계·Seoul 날짜 경계, 포함 날짜 범위와 겹치지 않는 분할.
- 가상 Claim 18건의 전체 수집·보강·POST, 재실행 시 18건 모두 변경 없음.
- 10일 60건을 낮은 limit로 요청해 자동 분할 후 누락·중복 없이 전송.
- 하루 한도 초과 및 하루 응답 크기 초과 시 해당 날짜 전송 차단.
- 제품 정보 누락, 잘못된 날짜, 요청 범위 밖의 접수일, schema 오류, 중복 업무 키.
- 제품 정보 변경 시 재전송, A→B→A 변경에도 서로 다른 전송 operation key.
- GET 재시도와 제품 캐시, POST 4xx·5xx·202·오류 JSON·유실 응답 처리.
- POST가 실제 가상 DB에 반영된 뒤 응답을 잃는 상황을 재현. 자동 재전송 차단 및 운영자 확인 후 복구.
- 실행 전 중지, 전송 중 종료, 시작 시 미완료 상태 복구.
- 두 실행기의 상호 배제와 실행기 종료 시 대기 실행기로 교대.
- 예약 유지·누락 간격 병합·중복 실행 방지·설정 변경 시 잘못된 대상으로 보내지 않기.
- 토큰 인증·외부 Origin 차단·Host 허용 목록·비밀 헤더 비노출.
- 웹 관리 API의 조회·큐 등록·미리보기·필터·페이지 조회·NDJSON 내보내기·재실행·확인 대기 처리.
- **별도 프로세스의 가상 API에 실제 HTTP socket으로 연결**하여 18건 전송 및 저장 확인.

라이브러리에서 TestClient의 향후 HTTPX 사용 변경과 AnyIO 별칭에 관한 deprecation 경고 2건이 발생합니다. 실패가 아니며 검증한 의존성 버전을 lock 파일에 고정했습니다.

## 실제 브라우저 검증

- 대시보드, 고정 기간 설정, 분할 단위, API 전송 버튼과 진행 로그 확인.
- 2025-01-01 ~ 2025-01-05를 2일 단위로 실행: 30건 처리, 기존 18건 제외 후 12건 추가.
- 행별 전송 JSON에서 `{"values": ...}` 및 날짜·제품 조합 확인.
- 1분 간격 예약을 저장한 뒤 **세 차례 실제 자동 실행** 확인: 매번 30건 처리, 오류 0건. 가상 대상 DB는 30건 유지.
- 검증 후 테스트 예약 비활성화.
- API 연결 확인 화면에서 Claim·제품 schema 응답 확인.
- 데스크톱과 390px 모바일 너비에서 화면 표시 및 가로 넘침 확인.
- 확인한 최종 화면의 브라우저 console error 없음.

## 배포 검증 범위

GitHub Actions의 **Ubuntu + Python 3.11/3.12/3.13 세 환경 모두 테스트·정적 검사·패키지 빌드가 성공했습니다.** 검증한 구현 커밋은 `628bf4c69bb5ba2340f8222e100a41f0f48d5ae5`이며 [실행 결과](https://github.com/wooch123/db_migration_byCodex/actions/runs/34344266498)에서 확인할 수 있습니다.

로컬 컴퓨터에는 Ubuntu/WSL과 Docker 실행 환경이 설치되어 있지 않아 systemd 서비스 등록과 Docker 실행은 현지에서 수행하지 않았습니다. 서비스 파일·배포 문서를 제공하며 실제 사내 Ubuntu 환경에서 네트워크·권한·인증서를 함께 확인해야 합니다.

**사내 API에는 접속하지 않았고 운영 DB에 전송하지 않았습니다.** 실제 응답 래퍼, 업무 키, 대상 API의 upsert 지원, null 처리, 성공 확인 응답 및 사내 인증 정보는 사내 전환 검증 대상입니다. [API 계약](api-contracts.md)과 [운영 가이드](operations.md)에 필요한 설정을 정리했습니다.
